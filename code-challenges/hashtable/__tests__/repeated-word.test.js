'use strict';

const repeatedWord = require('../repeatedWords/repeated-word.js');

describe('repeatedWord', () => {

  it ('returns first repeated word', () => {
    let expected = 'the';
    let  = insertShiftArray([ 2, 4, 6, 8], 5);
    expect(newArray).toEqual(expected);
  });
});
